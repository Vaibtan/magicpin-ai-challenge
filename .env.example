ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
LLM_PROVIDER=gemini
LLM_FALLBACK_PROVIDER=none
GEMINI_COMPOSE_MODEL=gemini-3-flash-preview
GEMINI_CLASSIFY_MODEL=gemini-3.1-flash-lite-preview
JUDGE_LLM_PROVIDER=gemini
JUDGE_LLM_MODEL=gemini-2.5-pro
# Set to 1 to load state_dump.json on startup. NEVER set in judge run.
BOT_DEV_MODE=0
# Optional override for run identifier in logs/run_{run_id}.jsonl
# Defaults to UTC timestamp at process start.
RUN_ID=